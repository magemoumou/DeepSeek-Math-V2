## 🎯 DeepSeekMath-V2三重验证机制Mermaid流程图设计

### 🔍 核心模型推理接口识别

通过代码分析，我识别出关键的模型推理调用环节：

1. **generate.py:24** - `self.client.chat.completions.create()` - 核心模型调用
2. **main.py:462** - `os.system(proof_gen_cmd)` - 证明生成命令调用
3. **main.py:492** - `os.system(proof_verification_cmd)` - 证明验证命令调用  
4. **main.py:523** - `os.system(meta_verification_cmd)` - 元验证命令调用

### 📋 流程图结构设计

流程图将包含以下关键元素：

1. **代码控制层** - 蓝色节点，表示Python代码逻辑控制
2. **模型推理层** - 红色节点，表示实际的LLM模型调用
3. **数据处理层** - 绿色节点，表示数据格式转换和处理
4. **存储层** - 灰色节点，表示文件读写操作
5. **决策层** - 黄色节点，表示条件判断和分支

### 🔄 与常规LLM的核心差异标注

将在流程图中特别标注：
- **单次vs多轮**: 常规LLM一次完成 vs DS-Math多轮迭代
- **验证机制**: 无验证 vs 三重验证
- **质量控制**: 无筛选 vs 严格质量管控
- **并行处理**: 单次调用 vs 批量并行处理